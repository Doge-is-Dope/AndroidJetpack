<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="movieViewModel"
            type="com.chunchiehliang.kotlin.demo.MovieViewModel.Movie" />
    </data>


    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="96dp">


        <ImageView
            android:id="@+id/image_recipe"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:background="@drawable/filter_sheet_background"
            android:contentDescription="@string/image_description_recipe"
            android:src="@drawable/ic_launcher_background"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/text_recipe_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_normal"
            android:layout_marginTop="@dimen/spacing_normal"
            android:text="@{movieViewModel.name}"
            android:textSize="20sp"
            app:layout_constraintBottom_toTopOf="@id/text_recipe_time"
            app:layout_constraintStart_toEndOf="@id/image_recipe"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Orange" />

        <TextView
            android:id="@+id/text_recipe_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/spacing_normal"
            android:layout_marginTop="@dimen/spacing_normal"
            android:layout_marginBottom="@dimen/spacing_normal"
            android:text="@{movieViewModel.time}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/image_recipe"
            app:layout_constraintTop_toBottomOf="@id/text_recipe_title"
            tools:text="橘" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
